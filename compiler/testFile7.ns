package main;

pub type List :: struct {
    data: $T interface{},
    data2: $T,
    next: List($T),
};

pub func() -> void {
    {{{{{{{};};};};};};};
    5;
    if(x == 0) {}
    let x := if(cond){ => 3; } else if(cond){ => 5; } else { => 7; };
}
